name: Auto Sync Projects

on:
  push:
    paths:
      - 'src/build-tweaks/**/control'
  workflow_dispatch:

jobs:
  sync-menu:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      workflows: write # Cấp quyền tối thượng để robot sửa được file .yml

    steps:
# ... các bước sau giữ nguyên ...
